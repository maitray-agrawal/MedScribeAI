# Smart India Hackathon (SIH 2026) Capability Matrix

**Project:** MedScribeAI — Sovereign AI-Powered Patient Case-Taking & Clinical Documentation Platform  
**Overall Readiness Classification:** **B. SIH DEMO READY WITH DECLARED LIMITATIONS**  
**Date of Audit:** September 12, 2026  
**Auditor:** Antigravity AI Engineering Validation Team  

---

## 1. Comprehensive Empirical Capability Matrix

Every capability claimed by MedScribeAI has been audited against physical code, test suites, runtime binaries, and host environment constraints. No capability is claimed without verifiable empirical evidence.

| # | Capability / Feature | Claimed Functionality | Empirical Status | Evidence & Test File | Declared Limitations & Honest Boundary |
|---|---|---|---|---|---|
| **1** | **Bilingual Speech ASR (IndicConformer)** | Sovereign on-device speech-to-text for 8 Indic languages (Hindi, Marathi, Gujarati, etc.) using INT8 quantized model. | **PARTIALLY VERIFIED** | `models/asr/indic-conformer/model.int8.onnx` (188 MB, SHA-256: `b99a0183...`), `test_human_asr_benchmark.py`, deep ONNX tensor contract verified. | Technical ONNX runtime inference is verified. **Empirical human WER accuracy is NOT claimed** due to lack of a diverse physical acoustic recording corpus in this testing environment. |
| **2** | **Document OCR (Tesseract Engine)** | Sovereign on-device OCR extraction from prescriptions, lab reports, and discharge summaries. | **UNAVAILABLE** | `src/ocr/ocrProvider.ts`, `backend/app/ocr/pipeline.py`, `backend/tests/test_ocr_pipeline.py`. Binary probe returns `available: false`. | Tesseract binary is **not installed on the host system**. The system fails cleanly with HTTP 422 `OCR_UNAVAILABLE` and emits zero fabricated clinical facts. Synthetic fallbacks were permanently eradicated. |
| **3** | **Deterministic Clinical NLP** | Multilingual entity extraction (symptoms, conditions, vitals, temporal durations) across Hindi, Marathi, English, and Hinglish. | **VALIDATED** | `multilingualClinicalNLP.test.ts` (156 tests), `test_nlp_extractor.py` (88 tests), `regressionPhase87.test.ts` (Tests 1, 2, 3). | Native Devanagari and Romanized transliteration supported. Tamil supported via lexical lexicon; complex syntactic Dravidian parsing is limited. |
| **4** | **Assertion & Negation Semantics** | Preserves linguistic negation ("fever nahi hai", "BP normal hai") as `NEGATED` and denies without losing evidence. | **VALIDATED** | `regressionPhase87.test.ts` (Test 1, 3, 13), `test_assertion_temporality_experiencer.py`. | Contrastive conjunctions ("X hai lekin Y nahi hai") extract dual facts with separate assertion values. |
| **5** | **Elicitation vs Assertion Separation (Allergies)** | Strictly separates patient inquiry from clinical finding. Never defaults missing allergy history to NKDA. | **VALIDATED** | `canonicalClinicalFacts.test.ts` (Tests 8, 9, 30), `regressionPhase87.test.ts` (Tests 6, 7), `test_behavioral_zero_fabrication.py`. | Unprompted allergy topics are preserved as `UNKNOWN` + `NOT_ELICITED` with zero evidence, completely preventing synthetic NKDA claims. |
| **6** | **Medication & Conflicting Evidence Handling** | Extracts medication dose, unit, frequency, and preserves conflicting prescription vs verbal discontinuation history. | **VALIDATED** | `clinicalConflictHandling.test.ts` (4 tests), `regressionPhase87.test.ts` (Tests 4, 5, 8). | Conflicting facts are stored side-by-side in `ClinicalFactStore.getAllConflicts()` with non-destructive versioning; system alerts physician instead of silently overwriting. |
| **7** | **AYUSH Constitutional Assessment** | Extracts Prakriti (Vata/Pitta/Kapha) and Agni with strict `PATIENT_REPORTED` vs `CLINICIAN_OBSERVED` provenance. | **VALIDATED** | `kioskAYUSH.test.tsx` (3 tests), `canonicalClinicalFacts.test.ts` (Tests 6, 7), `regressionPhase87.test.ts` (Test 11). | Patient-reported Prakriti is tagged with `experiencer: PATIENT` and cannot be projected as an objective clinician finding. |
| **8** | **Emergency Red-Flag Triage Alerts** | Real-time detection of high-risk clinical combinations (chest pain + dyspnea, thunderclap headache) with immediate triage notification. | **VALIDATED** | `redFlagRules.ts`, `emergencyTriageDetector.ts`, `regressionPhase87.test.ts` (Tests 12, 13), `test_safety_benchmark.py`. | False-positive resistant: negated symptoms ("seene me dard nahi hai") do not trigger emergency cardiac alerts. |
| **9** | **Universal Zero-Fabrication Evidence Gate** | Cryptographic/string-matching audit that rejects ungrounded diagnoses (GERD, Amlapitta), fake vitals, or unauthorized prescriptions. | **VALIDATED** | `src/clinical/evidenceGate.ts`, `backend/app/clinical/evidence_gate.py`, `regressionPhase87.test.ts` (Test 14). | Blocks any diagnosis or prescription not backed by a verbatim span from the source transcript or clinician authorization. |
| **10** | **FHIR R4 & ABDM Milestone Compliance** | Generates valid FHIR R4 Bundles (Encounter, Patient, Condition, Observation, MedicationStatement) for ABDM M1/M2 integration. | **VALIDATED** | `fhirConverter.test.tsx` (6 tests), `regressionPhase87.test.ts` (Test 15). | Unconfirmed pre-consultation intake drafts are tagged with `verificationStatus: 'unconfirmed'` and cannot be used as billable claims. |
| **11** | **Physician Approval State Machine** | Clinical notes follow strict state transitions: `AI_DRAFT` ➔ `REVIEWING` ➔ `APPROVED`. Material edits invalidate approval. | **VALIDATED** | `src-tauri/src/main.rs`, `SOAPNoteHeader.tsx`, `SOAPNoteView.tsx`, `regressionPhase87.test.ts` (Test 15, 17). | Prescription printing (`btn-print-rx`) and FHIR export (`btn-export-fhir`) are disabled while the note is in `AI_DRAFT` or `REVIEWING`. |
| **12** | **Patient Consent Gating** | Fine-grained consent tracking (voice capture, document upload, hospital data sharing). | **VALIDATED** | `KioskShell.tsx`, `FHIRExportModal.tsx`, `regressionPhase87.test.ts` (Test 16). | `hospitalSharing: false` completely blocks outbound synchronization, auto-push to hospital EMR, and external ABDM dispatch. |
| **13** | **Offline Local Engine Fallback** | Deterministic browser-side extraction engine that executes if backend fails or network is lost. | **VALIDATED** | `offlineLocalEngine.test.tsx` (9 tests), `resilienceAndEdgeCases.test.tsx` (13 tests). | Seamless automatic fallback upon network failure, quota exhaustion, or backend timeout without losing existing user input. |
| **14** | **Tauri Desktop & Sovereign Sidecar Lifecycle** | Cross-platform desktop application embedding a sovereign Python FastAPI sidecar child process. | **VALIDATED** | `src-tauri/src/main.rs`, `src-tauri/target/release/medscribe-ai-desktop.exe` (11.2 MB), `src-tauri/binaries/medscribe-backend-x86_64-pc-windows-msvc.exe` (210.8 MB). | Spawns child process on startup, polls loopback health, and issues SIGTERM / process termination on window close and app exit to guarantee zero orphan processes. |
| **15** | **Local Sovereign SQLite Database** | Offline transactional persistence for encounters, clinical facts, audit logs, and deferred sync queue. | **VALIDATED** | `backend/app/db/database.py`, `backend/tests/test_storage.py` (3 tests). | Enforces immutability: clinical facts belonging to approved encounters cannot be altered or deleted. |
| **16** | **Zero-Fabrication Behavioral Integrity** | Absolute eradication of hardcoded medical defaults across all application layers. | **VALIDATED** | `fabricationRegression.test.ts` (17 tests), `behavioralZeroFabrication.test.ts` (8 tests), complete repo regex scan. | No default GERD, Amlapitta, 120/80 mmHg, 37.0°C, or default NKDA exists in production execution paths. |
| **17** | **Empirical Human Speech ASR Benchmark** | Real human clinical audio corpus WER evaluation across regional dialects. | **NOT ATTEMPTED / DEFERRED** | `backend/tests/test_human_asr_benchmark.py` (`TestRealHumanAudioIngestion` marked `@pytest.mark.skip`). | Requires IRB-compliant multi-speaker clinical audio dataset collected in physical OPD settings. |
| **18** | **Real Physical Document OCR Validation** | End-to-end character accuracy and clinical entity extraction from physical camera-captured paper documents. | **UNAVAILABLE** | `backend/tests/test_ocr_pipeline.py` (18 skipped tests due to missing binary). | Blocked by missing Tesseract OCR binary on test runner host. |
| **19** | **Physical Air-Gapped Rural Field Deployment** | Multi-day deployment in an off-grid Primary Health Centre without internet access. | **PARTIALLY VERIFIED (LOCALHOST ISOLATED)** | `docs/network_audit.md`, `src/__tests__/airplaneModeE2E.test.ts`. | Code operates with zero egress, but physical multi-day rural hardware validation has not been performed. |
| **20** | **WiX Toolset MSI Installer Packaging** | Standalone Windows MSI installer containing both desktop frontend and Python sidecar. | **BLOCKED** | `docs/tauri_packaging.md`, `src-tauri/target/release/medscribe-ai-desktop.exe`. | WiX v3/v4 download is blocked by external network constraints in this environment. Desktop `.exe` compiles and runs directly. |

---

## 2. Summary of SIH 2026 Readiness

- **Production Core:** Complete, hardened, zero-fabrication clinical documentation pipeline.
- **Languages Supported:** Hindi, Marathi, Gujarati, English, Hinglish (code-switched).
- **Standards:** HL7 FHIR R4, ABDM M1/M2, SNOMED CT / ICD-10 canonical mapping, NAMASTE AYUSH ontology.
- **Safety Invariant:** Zero ungrounded diagnoses, zero fabricated vitals, zero unauthorized prescriptions.
- **Final Classification:** **B. SIH DEMO READY WITH DECLARED LIMITATIONS**.
